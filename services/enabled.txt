# runit services to enable
# Format: one service name per line
# These are symlinked from /etc/sv/<name> to /var/service/<name>

# Core system
# dbus and elogind are enabled by Void's base install - do not re-enable here
# as runit will immediately restart them mid-install and cause conflicts
seatd

# Networking
NetworkManager

# Bluetooth
bluetoothd

# Display manager
ly

# Printing
cupsd

# Power
acpid
tlp
